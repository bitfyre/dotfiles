- defaults:
    link:
      relink: true

- clean: ['~','~/.config']

- link:
    ~/.dotfiles: ''
    ~/.zshrc: zsh/zshrc
    ~/.zshenv: zsh/zshenv

- shell:
  - [git submodule update --init --recursive, Installing submodules]
