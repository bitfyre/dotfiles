[user]
	name = Alex Lemanski
[color]
	diff = auto
	status = auto
	branch = auto
[core]
	editor = vim
	excludesfile = ~/.gitignore_global
[alias]
	rb = rebase origin/master
  lg = !git --no-pager log -n 20 --branches --tags --graph \
      --date=relative \
      --pretty=format:\"%h  %ad  %C(red)%ae%Creset%x09%C(green)%d %Creset%s\"
[gerrit]
	createchangeid = true
[include]
	path = ~/.gitconfig.local
