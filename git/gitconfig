[user]
	name = Alex Lemanski
[color]
	diff = auto
	status = auto
	branch = auto
[core]
	editor = vim
	excludesfile = ~/.gitignore_global
[alias]
	rbm = rebase origin/master
	rbd = rebase origin/develop
  lg = !git --no-pager log -n 30 --branches --tags --graph \
    --date=relative \
    --pretty=format:\"%h  %ad  %C(red)%ae%Creset%x09%C(green)%d %Creset%s\"
[gerrit]
	createchangeid = true
[include]
	path = ~/.gitconfig.local
[hub]
	host = github.com
